# Miscellaneous Archived Scripts

Older scripts that do not fit a current flight or model category live here.

Prefer adding new reusable behavior to `tools/replay/replaylib/` and exposing it through a stable script in `tools/replay/scripts/`.


# Aero Model Experiments

Archived apogee-predictor, adaptive-drag, and CFD-scaling experiments.

Preferred entry point:

```bash
python3 tools/replay/scripts/compare_predictors.py --mode current-model
```

Run `python3 tools/replay/scripts/compare_predictors.py --list` to see the supported archived modes.

